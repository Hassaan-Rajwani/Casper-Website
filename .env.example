# Next.js server port (optional)
PORT=3000

# Firebase web app config
# Exact values for API key, App ID, and Messaging Sender ID must come from
# Firebase Console > Project Settings > General > Your apps.
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project-id.firebasestorage.app
NEXT_PUBLIC_FIREBASE_API_KEY=your-firebase-api-key
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your-messaging-sender-id
NEXT_PUBLIC_FIREBASE_APP_ID=your-firebase-app-id

# Optional admin credentials
NEXT_PUBLIC_ADMIN_EMAIL=
NEXT_PUBLIC_ADMIN_PASSWORD=

# tawk.to live chat (from Dashboard > Administration > Channels > Chat Widget > Direct Chat Link)
NEXT_PUBLIC_TAWK_PROPERTY_ID=your_property_id
NEXT_PUBLIC_TAWK_WIDGET_ID=your_widget_id

# Public contact email shown on the website
NEXT_PUBLIC_CONTACT_EMAIL=your-email@example.com
NEXT_PUBLIC_ORDER_NOTIFICATION_EMAIL=your-email@example.com

# Server email — Option A: Gmail SMTP (free, ~500 emails/day, no activation per customer)
# Google Account > Security > 2-Step Verification > App passwords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-google-app-password
MAIL_FROM=Casper <your-email@gmail.com>
ORDER_NOTIFICATION_EMAIL=your-email@gmail.com

# Server email — Option B: Resend (free tier ~100 emails/day, verify domain for production)
# RESEND_API_KEY=re_xxxxxxxx
# MAIL_FROM=Casper <orders@yourdomain.com>
# ORDER_NOTIFICATION_EMAIL=your-email@gmail.com

# Production site URL for order tracking links in customer emails
NEXT_PUBLIC_SITE_URL=https://your-site.vercel.app

# Server-only Firebase Admin credentials (Vercel env, NOT public — required for orders)
# Firebase Console > Project Settings > Service accounts > Generate new private key
# Vercel: paste the full JSON as one line, OR use FIREBASE_SERVICE_ACCOUNT_JSON_BASE64
FIREBASE_SERVICE_ACCOUNT_JSON=
